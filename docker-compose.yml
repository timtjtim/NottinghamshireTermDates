services:
  calendar:
    build: .
    ports:
      - "8484:8080"
    volumes:
      - cache_data:/app/cache
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  cache_data:
